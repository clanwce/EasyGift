<!DOCTYPE html>
<html>
<head>
  <style type="text/css">
  .change {
    color: #ffffff ;
  }
  .inline{
   display: inline;
  }
.new_notification{
  background-color:#98AFC7;
}

span.glyphicon-globe {
    font-size: 1.2em;
}

.dropdown-menu {
  width: 100%; 
}


body{
  background-color: #008080;
  color: #ffffff;
}

@media {
  html {
    overflow: -moz-scrollbars-vertical; 
    overflow-x: scroll; 
    overflow-y: scroll;
  }
}

  </style>
  <title><%= yield(:title) %></title>
  <!-- Additional header tags here -->
  <%= stylesheet_link_tag    "application", :media => "all" %>
  <%= javascript_include_tag "application"  %>
<script src="//js.pusher.com/2.2/pusher.min.js" type="text/javascript"></script>
  <%= csrf_meta_tags %>
</head>
<body>

    <!-- Fixed navbar -->
    <div class="navbar navbar-default navbar-fixed-top" role="navigation" style="background:#000000;">
      <div class="container">
        <div class="navbar-header">
          <button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-collapse" style="float:left">
            <span class="sr-only">Toggle navigation</span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
          </button>
          <div style="padding-top:3px; padding-bottom:3px">
          <a class="inline" href="/profile" style=""><%= image_tag 'EasyGifts.png', :alt => 'lgo', width:"170", height:"80", class: "inline" %>
          </a>
          </div>
        </div>
        <div id="user_navigation_bar" <% if current_user %> data-user-id=<%= current_user.id %> <% end %> class="navbar-collapse collapse">
          <% if current_page?("/gift_requests") %>

            <form class="navbar-form navbar-left" role="search" style="padding-left:140px; padding-top:20px" action="/gift_requests/searchresult" method = "post">
                  <div class="form-group" style="">
                    <input name="authenticity_token" type="hidden" value="<%= form_authenticity_token %>" />
                    <input type="text" id="giftrequestsearch" name="keyword" style="height: 28px; font-size: 12px;line-height: 12px; width: 320px" class="form-control" placeholder="Search">
                  </div>
                  <input type="submit" class="btn btn-default" style="height: 28px; font-size: 12px;line-height: 12px; width: 90px"value="Submit">
                </form>
            <% end %>
          <% if current_page?("/tags") %>
           <form class="navbar-form navbar-left" role="search" style="padding-left:190px; padding-top:10px" action="/tags/tag_search" method = "post">
                <div class="form-group">
                <input name="authenticity_token" type="hidden" value="<%= form_authenticity_token %>" />
                <input type="text" id="giftrequestsearch" name="keyword" style="height: 28px; font-size: 12px;line-height: 12px; width: 320px" class="form-control" placeholder="Search">
                  </div>
                <input type="submit" class="btn btn-default" style="height: 28px; font-size: 12px;line-height: 12px; width: 90px"value="Submit">
             </form>

            <% end %>
          <ul class="nav navbar-nav navbar-right" style="padding-top:5px;">
        <% if current_user %>
          <li id="user_notification_dropdown"> <a href="#" style= "" class="dropdown-toggle"  id="clickable" data-toggle="dropdown">
              <% @count = current_user.user_notifications.where(read: false).count %>
            <h4><span class="glyphicon glyphicon-globe"><span id="navigation_count" data-count=<%= @count %> class="badge" style="background-color:red;"><% if @count > 0 %><%= @count %> <% end %></span></span> </h4>
          </a>
          <ul id="user_notification_ul" class="dropdown-menu" style="width: 400px;overflow: auto; height: 500px" role="menu" id="notify" align="center" >
                                  <% @notifications = current_user.user_notifications.order("notification_id desc") %>
                    <% @notifications.each do |notification| %>
                    <% if !notification.read %>              
                      <li data-id=<%= notification.id %> class="new_notification" ><%= link_to notification.message, notification.notification_link_to_url %></li><hr>
                    <% else %>  
                      <li data-id=<%= notification.id %> ><%= link_to notification.message, notification.notification_link_to_url %></li><hr>
                    <% end %>
              <% end %>
          </ul>
          </li>
        <% end %>
        <li  style="color:#ffffff;padding-left:10px; padding-top:10px"><a href="/gift_requests">Home</a></li>
        <li class="dropdown" style="padding-top:15px">
          <a href="#" class="dropdown-toggle" style="padding-top:10px" data-toggle="dropdown">Settings <span class="caret"></span></a>
          <ul class="dropdown-menu" role="menu">
            <li ><a href="#" >Change Password</a></li>
            <li class="divider"></li>
            <li><a href="#">Separated link</a></li>
          </ul>
        </li>
        <li style="padding-top:10px"><a href="/users/sign_out" data-method="delete" rel="nofollow">Logout</a></li>
      </ul>
        </div><!--/.nav-collapse -->
      </div>
    </div>

    <h4>
<!--    #    <!-- <%= flash[:notice] if flash[:notice] %> 
   # 
   #   <% if flash[:notice] %>
   #    <%= flash[:notice] if flash[:notice] %>
   #  <% end %> -->
    </h4>
<!--     <p class="notice"><%= notice %></p> -->
    <p class="alert"><%= alert %></p>
  
   
<%= yield %>

</body>


  <footer>
    <div class="footer" align="center">
      CopyRights @  
      <a class="inline" style=""><%= image_tag 'EasyGiftsBlack.png', :alt => 'lgo', width:"150", height:"70", class: "inline" %></a>
        -  
      <a >Privacy Agreement</a>  
    </div>
  </footer>
</html>
