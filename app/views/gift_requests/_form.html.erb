<%= form_for(@gift_request) do |f| %>
  <% if @gift_request.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@gift_request.errors.count, "error") %> prohibited this gift_request from being saved:</h2>

      <ul>
      <% @gift_request.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
